# Buys Braai’s Cloudflare Worker preview v2.0

This package preserves the deployed v1.9 Home and About pages and completes the Catering page with customer-facing copy and two genuine recent-event photographs.

On Windows PowerShell:

```powershell
.\tools\cloudflare-worker.ps1
```

The script validates the site and performs a Wrangler dry run before asking for the exact confirmation `DEPLOY PREVIEW`.
